---
phase: 06-athletes-roster
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - src/v2/features/athletes/AthletesPage.tsx
  - src/v2/features/athletes/components/AthleteTable.tsx
  - src/v2/features/athletes/components/AthleteGrid.tsx
  - src/v2/features/athletes/components/AthleteFilters.tsx
  - src/v2/features/athletes/components/AthleteProfilePanel.tsx
  - src/v2/features/athletes/index.ts
  - src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Coach can view roster in table view with sortable columns"
    - "Coach can switch to grid view showing athlete cards"
    - "Coach can search athletes by name"
    - "Coach can filter by side preference and capabilities"
    - "Coach can view athlete profile with biometrics in slide-out panel"
  artifacts:
    - path: "src/v2/features/athletes/AthletesPage.tsx"
      provides: "Main athletes roster page"
      exports: ["AthletesPage"]
      min_lines: 100
    - path: "src/v2/features/athletes/components/AthleteTable.tsx"
      provides: "Virtualized athlete table"
      exports: ["AthleteTable"]
    - path: "src/v2/features/athletes/components/AthleteGrid.tsx"
      provides: "Athlete card grid view"
      exports: ["AthleteGrid"]
    - path: "src/v2/features/athletes/components/AthleteFilters.tsx"
      provides: "Search and filter controls"
      exports: ["AthleteFilters"]
    - path: "src/v2/features/athletes/components/AthleteProfilePanel.tsx"
      provides: "Slide-out athlete profile with edit form"
      exports: ["AthleteProfilePanel"]
  key_links:
    - from: "src/v2/features/athletes/AthletesPage.tsx"
      to: "src/v2/hooks/useAthletes.ts"
      via: "hook import"
      pattern: "import.*useAthletes"
    - from: "src/v2/features/athletes/components/AthleteTable.tsx"
      to: "src/v2/components/common/VirtualTable.tsx"
      via: "component import"
      pattern: "import.*VirtualTable"
---

<objective>
Build the Athletes page with roster display, search/filters, and profile editing.

Purpose: ATH-01 through ATH-06 - Complete roster management UI with view switching, search, filters, and biometrics editing.
Output: Feature-complete athletes page accessible at /app/coach/athletes with grid/list views, filters, and slide-out profile panel.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-athletes-roster/06-CONTEXT.md (user decisions)
@src/v2/pages/CoachFleet.tsx (page structure reference)
@src/v2/components/fleet/ShellsTable.tsx (table pattern)
@src/v2/components/common/CrudModal.tsx (modal pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Athletes page structure</name>
  <files>src/v2/features/athletes/AthletesPage.tsx, src/v2/features/athletes/index.ts</files>
  <action>
Create the main Athletes page with these features:

Page structure:
- Page header: "Athletes" title with athlete count badge
- View toggle: Table (list) | Grid buttons with icons (List, Grid3X3 from lucide-react)
- "Add Athlete" button (plus icon) - opens create form
- "Import CSV" button - opens CSV import modal (implemented in next plan)
- Filter bar: Search input + filter dropdowns
- Main content area: Either AthleteTable or AthleteGrid based on view mode
- Profile panel: Slide-out panel for viewing/editing athlete (Framer Motion AnimatePresence)

State management:
- viewMode: 'table' | 'grid' (persisted to localStorage)
- selectedAthleteId: string | null (for profile panel)
- filters: AthleteFilters object

Use useAthletes hook with filters for data fetching.
Use Zustand or simple React state for view preference.

Styling:
- Match CoachFleet page structure
- Use V2 design tokens (txt-primary, surface-elevated, etc.)
- Responsive: Stack filters on mobile, full width table/grid

Create barrel export in index.ts:
```typescript
export { AthletesPage } from './AthletesPage';
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/athletes/AthletesPage.tsx</verify>
  <done>AthletesPage renders with view toggle, add button, filters, and content area</done>
</task>

<task type="auto">
  <name>Task 2: Create AthleteTable and AthleteGrid components</name>
  <files>src/v2/features/athletes/components/AthleteTable.tsx, src/v2/features/athletes/components/AthleteGrid.tsx</files>
  <action>
AthleteTable.tsx:
- Uses VirtualTable component from common
- Columns: Name, Side, Scull, Cox, Height, Weight (sortable on all)
- Name column shows "FirstName LastName" format
- Side shows "Port", "Starboard", "Both", or "Cox" with colored badge
- Scull/Cox show checkmark or empty
- Height shows in cm, Weight in kg (or "--" if null)
- Row click opens profile panel
- Pass getRowKey={(a) => a.id}

AthleteGrid.tsx:
- CSS Grid: 4 columns on xl, 3 on lg, 2 on md, 1 on sm
- Athlete card shows:
  - Avatar circle with initials (first letter of first+last name)
  - Full name
  - Side badge
  - Capability icons (oar for scull, megaphone for cox)
- Card click opens profile panel
- Empty state when no athletes match filters

Both components receive props:
```typescript
interface ViewProps {
  athletes: Athlete[];
  onSelect: (athlete: Athlete) => void;
  sortColumn?: string;
  sortDirection?: 'asc' | 'desc';
  onSort?: (column: string) => void;
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/athletes/components/AthleteTable.tsx src/v2/features/athletes/components/AthleteGrid.tsx</verify>
  <done>Both view components render athletes, support selection, table supports sorting</done>
</task>

<task type="auto">
  <name>Task 3: Create AthleteFilters and AthleteProfilePanel components</name>
  <files>src/v2/features/athletes/components/AthleteFilters.tsx, src/v2/features/athletes/components/AthleteProfilePanel.tsx</files>
  <action>
AthleteFilters.tsx:
- Search input with Search icon (debounced 300ms)
- Side filter dropdown: All, Port, Starboard, Both, Cox
- Scull filter toggle/dropdown: All, Can Scull, Cannot Scull
- Cox filter toggle/dropdown: All, Can Cox, Cannot Cox
- Clear filters button when any filter active
- Responsive: Wrap on mobile

Props:
```typescript
interface AthleteFiltersProps {
  filters: AthleteFilters;
  onChange: (filters: AthleteFilters) => void;
  resultCount: number;
}
```

AthleteProfilePanel.tsx:
- Slide-out panel from right (Framer Motion slide animation)
- Panel width: 400px on desktop, full width on mobile
- Overlay behind panel (click to close)
- Header: Athlete name + close button
- Avatar section: Large initials circle
- Read-only section: Name, Email, Side, Capabilities
- Editable biometrics section (ATH-05, ATH-06):
  - Height (cm) - number input
  - Weight (kg) - number input
  - Side preference - select dropdown
  - Can Scull - toggle
  - Can Cox - toggle
- Edit mode toggle button
- Save/Cancel buttons in edit mode
- Uses react-hook-form + Zod for form validation
- Calls useAthletes().updateAthlete on save

Props:
```typescript
interface AthleteProfilePanelProps {
  athlete: Athlete | null;
  isOpen: boolean;
  onClose: () => void;
  onUpdate: (id: string, data: Partial<AthleteInput>) => void;
}
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/athletes/components/AthleteFilters.tsx src/v2/features/athletes/components/AthleteProfilePanel.tsx</verify>
  <done>Filters component works, profile panel shows athlete with edit form</done>
</task>

<task type="auto">
  <name>Task 4: Add route to App.jsx</name>
  <files>src/App.jsx</files>
  <action>
Add route for the Athletes page in the V2/app routes section:

1. Import AthletesPage (lazy loaded):
```javascript
const AthletesPage = lazy(() =>
  import('./v2/features/athletes').then(m => ({ default: m.AthletesPage }))
);
```

2. Add route under /app/coach:
```jsx
<Route path="athletes" element={<AthletesPage />} />
```

The route should be: /app/coach/athletes

Ensure it's inside the authenticated route protection and V2 layout.
  </action>
  <verify>grep -q "AthletesPage" src/App.jsx && grep -q "athletes" src/App.jsx</verify>
  <done>Route added, page accessible at /app/coach/athletes</done>
</task>

</tasks>

<verification>
1. All components compile: `npx tsc --noEmit`
2. Route exists in App.jsx
3. Manual test: Navigate to /app/coach/athletes
4. Features work: Search filters, view toggle, profile panel opens
</verification>

<success_criteria>
- Athletes page renders at /app/coach/athletes
- Table view shows all athletes with virtualization
- Grid view shows athlete cards
- View toggle persists preference
- Search filters athletes by name (debounced)
- Side/capability filters work
- Profile panel slides in on row/card click
- Biometrics editable and saves correctly
- ATH-01 through ATH-06 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-athletes-roster/06-04-SUMMARY.md`
</output>
