---
phase: 06-athletes-roster
plan: 06
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - src/v2/features/attendance/AttendancePage.tsx
  - src/v2/features/attendance/components/AttendanceRecorder.tsx
  - src/v2/features/attendance/components/AttendanceHistory.tsx
  - src/v2/features/attendance/components/TeamAttendanceSummary.tsx
  - src/v2/features/attendance/index.ts
  - src/App.jsx
autonomous: true

must_haves:
  truths:
    - "Coach can select a date and record attendance for all athletes"
    - "Coach can set status to present/late/excused/unexcused for each athlete"
    - "Coach can view attendance history for individual athletes"
    - "Coach can view team-wide attendance summary for date range"
  artifacts:
    - path: "src/v2/features/attendance/AttendancePage.tsx"
      provides: "Main attendance management page"
      exports: ["AttendancePage"]
      min_lines: 80
    - path: "src/v2/features/attendance/components/AttendanceRecorder.tsx"
      provides: "Daily attendance recording interface"
      exports: ["AttendanceRecorder"]
      min_lines: 100
    - path: "src/v2/features/attendance/components/AttendanceHistory.tsx"
      provides: "Individual athlete attendance history"
      exports: ["AttendanceHistory"]
    - path: "src/v2/features/attendance/components/TeamAttendanceSummary.tsx"
      provides: "Team-wide attendance statistics"
      exports: ["TeamAttendanceSummary"]
  key_links:
    - from: "src/v2/features/attendance/AttendancePage.tsx"
      to: "src/v2/hooks/useAttendance.ts"
      via: "hook import"
      pattern: "import.*useAttendance"
    - from: "src/v2/features/attendance/AttendancePage.tsx"
      to: "src/v2/hooks/useAthletes.ts"
      via: "hook import for athlete list"
      pattern: "import.*useAthletes"
---

<objective>
Build the Attendance tracking feature with daily recording and history views.

Purpose: ATT-01, ATT-02, ATT-03 - Enable coaches to record daily attendance and view historical data.
Output: Attendance page with date-based recording grid, athlete history view, and team summary statistics.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/v2/features/athletes/AthletesPage.tsx (page structure reference)
@src/v2/hooks/useAttendance.ts (attendance hooks)
@src/v2/components/availability/AvailabilityGrid.tsx (grid pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AttendancePage with tabs</name>
  <files>src/v2/features/attendance/AttendancePage.tsx, src/v2/features/attendance/index.ts</files>
  <action>
Create the main Attendance page with two main views:

Layout:
- Page header: "Attendance" title
- Date picker for selecting practice date
- Two tabs: "Record" (default) | "Summary"
- Tab content area

Record tab:
- Shows AttendanceRecorder component
- Allows recording attendance for selected date

Summary tab:
- Date range picker (last 7 days, 30 days, custom)
- Shows TeamAttendanceSummary component

State:
- selectedDate: Date (defaults to today)
- activeTab: 'record' | 'summary'
- dateRange: { start: Date, end: Date } (for summary view)

Use simple tab styling matching V2 design system.
Date picker: Use native input[type="date"] or a simple custom date picker.

Create barrel export in index.ts:
```typescript
export { AttendancePage } from './AttendancePage';
```
  </action>
  <verify>npx tsc --noEmit src/v2/features/attendance/AttendancePage.tsx</verify>
  <done>AttendancePage renders with tabs, date picker, and switches between record/summary views</done>
</task>

<task type="auto">
  <name>Task 2: Create AttendanceRecorder component</name>
  <files>src/v2/features/attendance/components/AttendanceRecorder.tsx</files>
  <action>
Create the daily attendance recording interface:

Display:
- List of all athletes (from useAthletes hook)
- For each athlete: Name + 4 status buttons (Present, Late, Excused, Unexcused)
- Current status highlighted with color:
  - Present: green
  - Late: yellow
  - Excused: blue
  - Unexcused: red
  - Not recorded: gray/outline

Features:
- Quick actions: "Mark All Present" button at top
- Search/filter to find specific athletes
- Notes field (optional) - click to expand for athlete-specific note
- Auto-save: Changes save automatically with debounce
- Manual save button for explicit confirmation
- Loading state while saving

Implementation:
- Fetch existing attendance for selected date using useAttendance(date)
- Track local changes in state
- Batch save changes using recordAttendance mutation
- Show "Saved" indicator when changes persist

Props:
```typescript
interface AttendanceRecorderProps {
  date: string; // ISO date string YYYY-MM-DD
}
```

Styling:
- Compact row layout for many athletes
- Mobile-friendly: Stack status buttons on narrow screens
- Use color-coded badges/buttons for status
  </action>
  <verify>npx tsc --noEmit src/v2/features/attendance/components/AttendanceRecorder.tsx</verify>
  <done>Recorder shows all athletes, status buttons work, auto-saves changes</done>
</task>

<task type="auto">
  <name>Task 3: Create AttendanceHistory and TeamAttendanceSummary components</name>
  <files>src/v2/features/attendance/components/AttendanceHistory.tsx, src/v2/features/attendance/components/TeamAttendanceSummary.tsx</files>
  <action>
AttendanceHistory.tsx (for athlete profile panel):
- Shows attendance history for a single athlete
- Calendar-style view or simple list
- Color-coded by status
- Shows summary stats: X% attendance rate, Y present, Z late, etc.
- Date range: Last 30 days by default

Props:
```typescript
interface AttendanceHistoryProps {
  athleteId: string;
  startDate?: string;
  endDate?: string;
}
```

Uses useAthleteAttendance hook.

TeamAttendanceSummary.tsx:
- Team-wide attendance statistics for date range
- Table view: One row per athlete showing:
  - Name
  - Present count
  - Late count
  - Excused count
  - Unexcused count
  - Attendance rate (present + late / total)
- Sortable by any column
- Team totals row at bottom
- Visual: Color gradient for attendance rate (green = good, red = poor)

Props:
```typescript
interface TeamAttendanceSummaryProps {
  startDate: string;
  endDate: string;
}
```

Uses useAttendance with date range for aggregated data.

Both components should handle loading and empty states gracefully.
  </action>
  <verify>npx tsc --noEmit src/v2/features/attendance/components/AttendanceHistory.tsx src/v2/features/attendance/components/TeamAttendanceSummary.tsx</verify>
  <done>History shows athlete's attendance, summary shows team-wide stats with sorting</done>
</task>

<task type="auto">
  <name>Task 4: Add route and integrate with athlete profile</name>
  <files>src/App.jsx, src/v2/features/athletes/components/AthleteProfilePanel.tsx</files>
  <action>
Add route for Attendance page:

1. Import AttendancePage (lazy loaded):
```javascript
const AttendancePage = lazy(() =>
  import('./v2/features/attendance').then(m => ({ default: m.AttendancePage }))
);
```

2. Add route under /app/coach:
```jsx
<Route path="attendance" element={<AttendancePage />} />
```

Route: /app/coach/attendance

Integrate with athlete profile panel:
- Add "Attendance" tab/section in AthleteProfilePanel
- Show AttendanceHistory component for the selected athlete
- Include attendance summary stats in profile header
  </action>
  <verify>grep -q "AttendancePage" src/App.jsx && grep -q "attendance" src/App.jsx</verify>
  <done>Route added, attendance visible in athlete profile panel</done>
</task>

</tasks>

<verification>
1. All components compile: `npx tsc --noEmit`
2. Route exists: /app/coach/attendance accessible
3. Record view: Can mark athletes present/late/excused/unexcused
4. Changes save: Verify with page refresh
5. Summary view: Shows team stats for date range
6. Profile integration: Athlete attendance history visible in profile panel
</verification>

<success_criteria>
- Coach can record attendance for any date
- Status options: present, late, excused, unexcused
- Quick "Mark All Present" action works
- Attendance history visible per athlete
- Team summary shows aggregated stats
- ATT-01, ATT-02, ATT-03 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-athletes-roster/06-06-SUMMARY.md`
</output>
