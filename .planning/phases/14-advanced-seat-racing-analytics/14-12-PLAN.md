---
phase: 14-advanced-seat-racing-analytics
plan: 12
type: execute
wave: 5
depends_on: ["14-11"]
files_modified:
  - src/v2/pages/SeatRacingPage.tsx
  - src/v2/components/layout/WorkspaceSidebar.tsx
autonomous: false

must_haves:
  truths:
    - "Seat Racing page has links to advanced rankings"
    - "Sidebar navigation includes advanced analytics"
    - "User can navigate complete workflow"
  artifacts:
    - path: "src/v2/pages/SeatRacingPage.tsx"
      provides: "Updated seat racing page with links"
      contains: "advanced-rankings"
  key_links:
    - from: "src/v2/pages/SeatRacingPage.tsx"
      to: "AdvancedRankingsPage"
      via: "navigation link"
      pattern: "Link.*advanced-rankings"
---

<objective>
Update navigation to include advanced analytics features and verify complete user workflow.

Purpose: Ensure users can discover and access all Phase 14 features
Output: Updated navigation, human verification of complete workflow
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/v2/pages/SeatRacingPage.tsx
@src/v2/components/layout/WorkspaceSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SeatRacingPage with advanced analytics links</name>
  <files>src/v2/pages/SeatRacingPage.tsx</files>
  <action>
Add cards/links to the SeatRacingPage that navigate to advanced analytics features.

Find the existing page content and add a section for advanced features:

```tsx
// Add to imports
import { Link } from 'react-router-dom';
import {
  ChartBarIcon,
  CalendarIcon,
  ShareIcon,
  BeakerIcon
} from '@heroicons/react/24/outline';

// Add this section after the existing content (rankings table, session list, etc.):

{/* Advanced Analytics Section */}
<div className="mt-8">
  <h2 className="text-lg font-semibold text-txt-primary mb-4">
    Advanced Analytics
  </h2>
  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
    <FeatureCard
      to="/app/coach/seat-racing/advanced-rankings"
      icon={ChartBarIcon}
      title="Advanced Rankings"
      description="Bradley-Terry model, composite rankings, and comparison graphs"
    />
    <FeatureCard
      to="/app/coach/seat-racing/matrix-planner"
      icon={CalendarIcon}
      title="Matrix Planner"
      description="Generate optimal swap schedules for seat racing sessions"
    />
    <FeatureCard
      to="/app/coach/seat-racing/advanced-rankings#comparison-graph"
      icon={ShareIcon}
      title="Comparison Graph"
      description="Visualize which athletes have been compared"
    />
    <FeatureCard
      to="/app/coach/seat-racing/advanced-rankings#probability"
      icon={BeakerIcon}
      title="Win Probability"
      description="Heatmap of predicted head-to-head outcomes"
    />
  </div>
</div>

// Add this helper component (can be inside the file or extracted):
function FeatureCard({
  to,
  icon: Icon,
  title,
  description
}: {
  to: string;
  icon: React.ElementType;
  title: string;
  description: string;
}) {
  return (
    <Link
      to={to}
      className="block p-4 bg-surface-secondary rounded-lg border border-bdr-primary hover:border-accent-primary hover:shadow-md transition-all group"
    >
      <div className="flex items-start gap-3">
        <div className="p-2 bg-accent-primary/10 rounded-lg group-hover:bg-accent-primary/20 transition-colors">
          <Icon className="w-5 h-5 text-accent-primary" />
        </div>
        <div>
          <h3 className="font-medium text-txt-primary group-hover:text-accent-primary transition-colors">
            {title}
          </h3>
          <p className="text-sm text-txt-secondary mt-1">{description}</p>
        </div>
      </div>
    </Link>
  );
}
```
  </action>
  <verify>
- Feature cards render on SeatRacingPage
- Links navigate to correct pages
- Cards have hover effects
  </verify>
  <done>
SeatRacingPage updated with advanced analytics feature cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar navigation (optional enhancement)</name>
  <files>src/v2/components/layout/WorkspaceSidebar.tsx</files>
  <action>
Check if the WorkspaceSidebar has a seat racing section. If it does, add a sub-item for Advanced Rankings.

Look for the seat racing navigation item and add a nested item:

```typescript
// Find the seat racing nav item (might look like this):
{
  label: 'Seat Racing',
  path: '/app/coach/seat-racing',
  icon: ScaleIcon, // or similar
  // Add children if the nav supports it:
  children: [
    {
      label: 'Sessions',
      path: '/app/coach/seat-racing',
    },
    {
      label: 'Advanced Rankings',
      path: '/app/coach/seat-racing/advanced-rankings',
    },
    {
      label: 'Matrix Planner',
      path: '/app/coach/seat-racing/matrix-planner',
    },
  ],
}
```

If the sidebar doesn't support nested navigation, skip this task - the feature cards on the SeatRacingPage provide adequate discoverability.
  </action>
  <verify>
- Check if sidebar supports nested items
- If yes, verify items appear
- If no, confirm feature cards are sufficient
  </verify>
  <done>
Sidebar updated with nested items OR confirmed feature cards are sufficient.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 14: Advanced Seat Racing Analytics
- Bradley-Terry statistical ranking model
- Matrix session planner with Latin Square scheduling
- Comparison graph visualization
- Probability matrix heatmap
- Composite rankings with configurable weights
- Side-specific ELO tracking
- Two new pages with tabbed interface
- Navigation integration
  </what-built>
  <how-to-verify>
1. **Navigate to Seat Racing page** (http://localhost:3001/app/coach/seat-racing)
   - Verify: "Advanced Analytics" section appears with 4 feature cards
   - Click "Advanced Rankings" card

2. **Test Advanced Rankings page** (/app/coach/seat-racing/advanced-rankings)
   - Verify: 5 tabs appear (Composite, Bradley-Terry, By Side, Comparisons, Win Probability)
   - Click each tab and verify:
     - Composite: Weight profile selector + rankings table with breakdown
     - Bradley-Terry: Rankings with confidence interval bars
     - By Side: Port/Starboard/Cox filter buttons + rankings
     - Comparisons: Network graph visualization (if seat race data exists)
     - Win Probability: Heatmap matrix (if seat race data exists)

3. **Test Matrix Planner page** (/app/coach/seat-racing/matrix-planner)
   - Verify: Instructions and planner wizard appear
   - Select 4+ athletes (use side filter if desired)
   - Choose boat class (e.g., "Pair (2-)")
   - Click "Generate Schedule"
   - Verify: Schedule appears in Grid View
   - Click "Timeline View" tab
   - Verify: Piece-by-piece breakdown appears

4. **Test composite ranking weights**
   - On Composite tab, change weight profile dropdown
   - Verify: Rankings update
   - Click slider icon to expand custom weights
   - Adjust sliders
   - Verify: Weights always sum to 100%

5. **Test comparison graph** (if seat race data exists)
   - On Comparisons tab, verify nodes appear for athletes
   - Verify: Edges connect compared athletes
   - If gaps exist, verify gap list shows missing comparisons

6. **Visual design check**
   - Verify: All components match "Precision Instrument" design
   - Verify: Dark theme renders correctly
   - Verify: Loading states show skeletons
   - Verify: Error states show helpful messages
  </how-to-verify>
  <resume-signal>
Type "approved" if all features work correctly, or describe specific issues to fix.
  </resume-signal>
</task>

</tasks>

<verification>
- All pages accessible and functional
- Navigation links work
- Components handle loading/error states
- Visual design is consistent
</verification>

<success_criteria>
1. SeatRacingPage has advanced analytics section
2. All new pages accessible via navigation
3. User can complete full workflow:
   - View advanced rankings
   - Plan matrix session
   - Generate swap schedule
   - Understand comparison coverage
4. Human verification passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-advanced-seat-racing-analytics/14-12-SUMMARY.md`
</output>
