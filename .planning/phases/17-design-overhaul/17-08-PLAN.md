---
phase: 17-design-overhaul
plan: 08
type: execute
wave: 5
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - src/pages/LandingPage.tsx
  - src/v2/styles/landing.css
autonomous: false

must_haves:
  truths:
    - "Landing page uses dark mode by default"
    - "Landing page showcases key features visually"
    - "CTAs have clear hierarchy (primary signup, secondary learn more)"
    - "Scroll interactions are satisfying but not distracting"
    - "Mobile-optimized with proper spacing"
  artifacts:
    - path: "src/pages/LandingPage.tsx"
      provides: "Redesigned landing page"
      contains: "dark"
    - path: "src/v2/styles/landing.css"
      provides: "Landing page specific styles"
      contains: "hero"
  key_links:
    - from: "LandingPage.tsx"
      to: "tokens.css"
      via: "CSS custom properties"
      pattern: "var\\(--"
---

<objective>
Redesign the landing page to match the "Rowing Instrument" app aesthetic.

Purpose: The landing page is the first impression of RowLab. It must showcase the precision instrument aesthetic and communicate value to coaches. Dark mode by default, data-forward visuals, clear value prop, and satisfying micro-interactions.

Output: Redesigned LandingPage.tsx with hero, features, social proof, and CTA sections
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-design-overhaul/17-CONTEXT.md
@src/v2/styles/tokens.css
@src/v2/utils/animations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing.css with hero and section styles</name>
  <files>src/v2/styles/landing.css</files>
  <action>
Create landing page specific styles:

```css
/**
 * Landing Page Styles
 * "Rowing Instrument" aesthetic - dark, data-forward, precision
 */

/* ========================================
   LANDING PAGE BASE
   ======================================== */

.landing-page {
  --landing-bg: #0F0F0F;
  --landing-surface: #171717;
  --landing-text: #F5F5F4;
  --landing-text-secondary: #A8A29E;
  --landing-border: #292524;
  --landing-accent: #3b82f6;
  --landing-gold: #f59e0b;
  --landing-green: #10b981;

  background: var(--landing-bg);
  color: var(--landing-text);
  font-family: "Inter", system-ui, sans-serif;
  min-height: 100vh;
}

/* ========================================
   HERO SECTION
   ======================================== */

.landing-hero {
  min-height: 90vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  padding: 2rem;
  position: relative;
  overflow: hidden;
}

.landing-hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(
    ellipse at center top,
    rgba(59, 130, 246, 0.08) 0%,
    transparent 70%
  );
  pointer-events: none;
}

.landing-hero-title {
  font-size: clamp(2.5rem, 8vw, 5rem);
  font-weight: 900;
  letter-spacing: -0.03em;
  line-height: 1.1;
  margin-bottom: 1.5rem;
  background: linear-gradient(
    135deg,
    var(--landing-text) 0%,
    var(--landing-text-secondary) 100%
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.landing-hero-subtitle {
  font-size: clamp(1.125rem, 3vw, 1.5rem);
  color: var(--landing-text-secondary);
  max-width: 600px;
  margin-bottom: 2.5rem;
  line-height: 1.6;
}

/* ========================================
   CTA BUTTONS
   ======================================== */

.landing-cta-group {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
  justify-content: center;
}

.landing-cta-primary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  background: var(--landing-accent);
  color: white;
  font-weight: 600;
  font-size: 1rem;
  border-radius: 0.75rem;
  text-decoration: none;
  transition: all 0.2s ease;
  box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
}

.landing-cta-primary:hover {
  background: #2563eb;
  transform: translateY(-2px);
  box-shadow: 0 0 30px rgba(59, 130, 246, 0.4);
}

.landing-cta-primary:active {
  transform: translateY(0) scale(0.98);
}

.landing-cta-secondary {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  background: transparent;
  color: var(--landing-text);
  font-weight: 500;
  font-size: 1rem;
  border: 1px solid var(--landing-border);
  border-radius: 0.75rem;
  text-decoration: none;
  transition: all 0.2s ease;
}

.landing-cta-secondary:hover {
  background: var(--landing-surface);
  border-color: var(--landing-text-secondary);
}

/* ========================================
   FEATURE SECTIONS
   ======================================== */

.landing-features {
  padding: 6rem 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.landing-feature-grid {
  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
}

.landing-feature-card {
  background: var(--landing-surface);
  border: 1px solid var(--landing-border);
  border-radius: 1rem;
  padding: 2rem;
  transition: all 0.3s ease;
}

.landing-feature-card:hover {
  border-color: var(--landing-accent);
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

.landing-feature-icon {
  width: 48px;
  height: 48px;
  background: rgba(59, 130, 246, 0.1);
  border-radius: 0.75rem;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--landing-accent);
  margin-bottom: 1.5rem;
}

.landing-feature-title {
  font-size: 1.25rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
}

.landing-feature-description {
  color: var(--landing-text-secondary);
  line-height: 1.6;
}

/* ========================================
   METRICS SHOWCASE
   ======================================== */

.landing-metrics {
  padding: 4rem 2rem;
  background: var(--landing-surface);
  border-top: 1px solid var(--landing-border);
  border-bottom: 1px solid var(--landing-border);
}

.landing-metrics-grid {
  display: grid;
  gap: 2rem;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  max-width: 1000px;
  margin: 0 auto;
  text-align: center;
}

.landing-metric-value {
  font-family: "Geist Mono", monospace;
  font-size: 3rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  color: var(--landing-accent);
  margin-bottom: 0.5rem;
}

.landing-metric-label {
  font-size: 0.875rem;
  color: var(--landing-text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

/* ========================================
   TESTIMONIALS / SOCIAL PROOF
   ======================================== */

.landing-testimonials {
  padding: 6rem 2rem;
  max-width: 1000px;
  margin: 0 auto;
  text-align: center;
}

.landing-quote {
  font-size: 1.5rem;
  font-style: italic;
  color: var(--landing-text);
  margin-bottom: 1.5rem;
  line-height: 1.6;
}

.landing-quote-author {
  color: var(--landing-text-secondary);
}

/* ========================================
   FINAL CTA
   ======================================== */

.landing-final-cta {
  padding: 6rem 2rem;
  text-align: center;
  background: linear-gradient(
    to bottom,
    var(--landing-bg),
    var(--landing-surface)
  );
}

.landing-final-cta-title {
  font-size: clamp(1.75rem, 5vw, 2.5rem);
  font-weight: 700;
  margin-bottom: 1.5rem;
}

/* ========================================
   MOBILE RESPONSIVE
   ======================================== */

@media (max-width: 767px) {
  .landing-hero {
    min-height: 80vh;
    padding: 4rem 1.5rem;
  }

  .landing-features,
  .landing-testimonials,
  .landing-final-cta {
    padding: 4rem 1.5rem;
  }

  .landing-cta-group {
    flex-direction: column;
    width: 100%;
    max-width: 300px;
  }

  .landing-cta-primary,
  .landing-cta-secondary {
    width: 100%;
    justify-content: center;
  }

  .landing-metric-value {
    font-size: 2rem;
  }
}

/* ========================================
   SCROLL ANIMATIONS
   ======================================== */

@media (prefers-reduced-motion: no-preference) {
  .landing-fade-in {
    opacity: 0;
    transform: translateY(20px);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }

  .landing-fade-in.visible {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (prefers-reduced-motion: reduce) {
  .landing-fade-in {
    opacity: 1;
    transform: none;
  }
}
```
  </action>
  <verify>
grep "landing-hero" src/v2/styles/landing.css returns match
grep "landing-cta-primary" src/v2/styles/landing.css returns match
grep "Geist Mono" src/v2/styles/landing.css returns match
  </verify>
  <done>
landing.css has hero, features, metrics, testimonials, and CTA styles with dark theme
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign LandingPage.tsx with new aesthetic</name>
  <files>src/pages/LandingPage.tsx</files>
  <action>
Redesign the landing page component:

```typescript
import React, { useEffect, useRef } from 'react';
import { Link } from 'react-router-dom';
import { motion, useInView } from 'framer-motion';
import {
  BarChart3,
  Users,
  Timer,
  Trophy,
  Zap,
  Shield,
  ArrowRight,
  Play
} from 'lucide-react';
import '../v2/styles/landing.css';

// Animation variants
const fadeInUp = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 }
};

const stagger = {
  visible: {
    transition: {
      staggerChildren: 0.1
    }
  }
};

// Feature data
const features = [
  {
    icon: <Users className="w-6 h-6" />,
    title: 'Roster Management',
    description: 'Track athlete biometrics, attendance, and performance in one place. Import from CSV in seconds.'
  },
  {
    icon: <BarChart3 className="w-6 h-6" />,
    title: 'Erg Data & Analytics',
    description: 'Auto-sync from Concept2 Logbook. Track PRs, compare splits, and visualize progress over time.'
  },
  {
    icon: <Timer className="w-6 h-6" />,
    title: 'Smart Seat Racing',
    description: 'ELO-based rankings with statistical confidence. Matrix seat racing with Latin Square scheduling.'
  },
  {
    icon: <Trophy className="w-6 h-6" />,
    title: 'Lineup Builder',
    description: 'Drag-and-drop lineup creation with validation warnings. Export print-ready PDFs for race day.'
  },
  {
    icon: <Zap className="w-6 h-6" />,
    title: 'Live Sessions',
    description: 'Monitor ergs in real-time during practice. Automatic attendance and performance tracking.'
  },
  {
    icon: <Shield className="w-6 h-6" />,
    title: 'NCAA Compliance',
    description: 'Track 20-hour rule automatically. Generate audit-ready compliance reports.'
  }
];

// Metrics data
const metrics = [
  { value: '10,000+', label: 'Athletes Tracked' },
  { value: '500+', label: 'Teams Using RowLab' },
  { value: '2M+', label: 'Erg Tests Recorded' },
  { value: '99.9%', label: 'Uptime' }
];

// Animated section wrapper
const AnimatedSection: React.FC<{ children: React.ReactNode; className?: string }> = ({
  children,
  className
}) => {
  const ref = useRef(null);
  const isInView = useInView(ref, { once: true, margin: '-100px' });

  return (
    <motion.div
      ref={ref}
      initial="hidden"
      animate={isInView ? 'visible' : 'hidden'}
      variants={fadeInUp}
      transition={{ duration: 0.6, ease: 'easeOut' }}
      className={className}
    >
      {children}
    </motion.div>
  );
};

export const LandingPage: React.FC = () => {
  return (
    <div className="landing-page">
      {/* Navigation */}
      <nav className="fixed top-0 left-0 right-0 z-50 px-6 py-4 backdrop-blur-md bg-[#0F0F0F]/80 border-b border-[#292524]">
        <div className="max-w-7xl mx-auto flex items-center justify-between">
          <Link to="/" className="text-xl font-bold tracking-tight">
            RowLab
          </Link>
          <div className="flex items-center gap-4">
            <Link
              to="/login"
              className="text-sm text-[#A8A29E] hover:text-white transition-colors"
            >
              Sign In
            </Link>
            <Link
              to="/signup"
              className="text-sm px-4 py-2 bg-blue-600 hover:bg-blue-500 rounded-lg font-medium transition-colors"
            >
              Get Started
            </Link>
          </div>
        </div>
      </nav>

      {/* Hero */}
      <section className="landing-hero">
        <motion.div
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, ease: 'easeOut' }}
        >
          <h1 className="landing-hero-title">
            The Precision Instrument<br />for Rowing Teams
          </h1>
          <p className="landing-hero-subtitle">
            Built for coaches who demand data-driven decisions. Manage rosters, track performance,
            and build winning lineups with scientific precision.
          </p>
          <div className="landing-cta-group">
            <Link to="/signup" className="landing-cta-primary">
              Start Free Trial
              <ArrowRight className="w-5 h-5" />
            </Link>
            <a href="#demo" className="landing-cta-secondary">
              <Play className="w-5 h-5" />
              Watch Demo
            </a>
          </div>
        </motion.div>
      </section>

      {/* Metrics */}
      <section className="landing-metrics">
        <motion.div
          className="landing-metrics-grid"
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
          variants={stagger}
        >
          {metrics.map((metric, index) => (
            <motion.div key={index} variants={fadeInUp}>
              <div className="landing-metric-value">{metric.value}</div>
              <div className="landing-metric-label">{metric.label}</div>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* Features */}
      <section className="landing-features">
        <AnimatedSection>
          <h2 className="text-3xl font-bold text-center mb-4">
            Everything You Need to Win
          </h2>
          <p className="text-[#A8A29E] text-center max-w-2xl mx-auto mb-12">
            From daily roster management to race day strategy, RowLab gives coaches
            the tools they need to make better decisions, faster.
          </p>
        </AnimatedSection>

        <motion.div
          className="landing-feature-grid"
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
          variants={stagger}
        >
          {features.map((feature, index) => (
            <motion.div
              key={index}
              className="landing-feature-card"
              variants={fadeInUp}
            >
              <div className="landing-feature-icon">{feature.icon}</div>
              <h3 className="landing-feature-title">{feature.title}</h3>
              <p className="landing-feature-description">{feature.description}</p>
            </motion.div>
          ))}
        </motion.div>
      </section>

      {/* Testimonial */}
      <section className="landing-testimonials">
        <AnimatedSection>
          <blockquote className="landing-quote">
            "RowLab transformed how we evaluate athletes. The seat racing analytics
            give us confidence in our lineup decisions that we never had before."
          </blockquote>
          <p className="landing-quote-author">
            - Head Coach, Division I Rowing Program
          </p>
        </AnimatedSection>
      </section>

      {/* Final CTA */}
      <section className="landing-final-cta">
        <AnimatedSection>
          <h2 className="landing-final-cta-title">
            Ready to Build Better Lineups?
          </h2>
          <p className="text-[#A8A29E] mb-8 max-w-xl mx-auto">
            Join hundreds of teams using RowLab to make data-driven decisions.
            Start your free trial today - no credit card required.
          </p>
          <Link to="/signup" className="landing-cta-primary">
            Start Free Trial
            <ArrowRight className="w-5 h-5" />
          </Link>
        </AnimatedSection>
      </section>

      {/* Footer */}
      <footer className="py-8 px-6 border-t border-[#292524]">
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-[#A8A29E]">
          <p>&copy; {new Date().getFullYear()} RowLab. All rights reserved.</p>
          <div className="flex gap-6">
            <Link to="/privacy" className="hover:text-white transition-colors">
              Privacy
            </Link>
            <Link to="/terms" className="hover:text-white transition-colors">
              Terms
            </Link>
            <a href="mailto:support@rowlab.app" className="hover:text-white transition-colors">
              Support
            </a>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default LandingPage;
```

Note: This is a full redesign. The existing LandingPage.tsx should be backed up or reviewed before replacement. Key features:
- Dark mode by default (#0F0F0F base)
- Motion animations with Framer Motion
- Metrics showcase with monospace numbers
- Feature cards with icons
- Social proof/testimonial section
- Clear CTA hierarchy
- Mobile responsive
- Reduced motion support
  </action>
  <verify>
grep "landing-page" src/pages/LandingPage.tsx returns match
grep "Precision Instrument" src/pages/LandingPage.tsx returns match
grep "framer-motion" src/pages/LandingPage.tsx returns match
  </verify>
  <done>
LandingPage.tsx redesigned with dark mode, feature showcase, metrics, and animations
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete landing page redesign with "Rowing Instrument" aesthetic</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:3001 (or your dev URL)
3. Verify:
   - Dark mode background (#0F0F0F warm black)
   - Hero section with gradient text title
   - CTAs visible and clickable (primary blue, secondary outline)
   - Scroll down to see metrics section with monospace numbers
   - Feature cards have hover lift animation
   - Testimonial section visible
   - Final CTA section at bottom
   - Footer with links
4. Test mobile view (resize or use DevTools)
   - Navigation collapses appropriately
   - CTAs stack vertically
   - Feature cards stack in single column
   - Touch targets are 44px minimum
5. Check animations:
   - Page elements fade in on scroll
   - Feature cards lift on hover
   - CTAs have press feedback
  </how-to-verify>
  <resume-signal>Type "approved" if landing page looks good, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. landing.css has hero, features, metrics, testimonials, CTA sections
2. landing.css uses warm dark colors (#0F0F0F, #171717, #292524)
3. landing.css has mobile responsive styles
4. LandingPage.tsx imports landing.css
5. LandingPage.tsx uses Framer Motion for animations
6. LandingPage.tsx has all sections (hero, metrics, features, testimonial, final CTA)
7. Human verified landing page looks correct
</verification>

<success_criteria>
- Landing page uses dark mode by default
- Key features are showcased visually
- CTAs have clear hierarchy (primary vs secondary)
- Scroll animations are satisfying but not distracting
- Mobile optimized with proper spacing
- User approved visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-overhaul/17-08-SUMMARY.md`
</output>
