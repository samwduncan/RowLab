---
phase: 17-design-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/styles/tokens.css
  - src/v2/styles/typography.css
  - index.html
autonomous: true

must_haves:
  truths:
    - "Headings use Inter font family"
    - "Data numbers use Geist Mono with tabular-nums"
    - "Large metrics (48-72px) feel like looking at a stroke coach display"
  artifacts:
    - path: "src/v2/styles/tokens.css"
      provides: "Font family CSS variables"
      contains: "--font-display: \"Inter\""
    - path: "src/v2/styles/typography.css"
      provides: "Typography utility classes"
      contains: "text-metric"
    - path: "index.html"
      provides: "Google Fonts link tags"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "typography.css"
      to: "tokens.css"
      via: "font-family CSS variables"
      pattern: "var\\(--font-"
---

<objective>
Implement the "Rowing Instrument" typography system with Inter for headings/body and Geist Mono for data display.

Purpose: Typography is critical for the "precision instrument" feel. Data-forward design requires monospace numbers that align like a stroke coach display. Inter provides clean, modern headings while Geist Mono gives crisp tabular numbers.

Output: Font loading via Google Fonts, updated font tokens, new typography utility classes including large metrics
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-design-overhaul/17-CONTEXT.md
@src/v2/styles/tokens.css
@src/v2/styles/typography.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add font loading to index.html</name>
  <files>index.html</files>
  <action>
Add Google Fonts preconnect and stylesheet links for Inter and Geist Mono in the head section:

```html
<!-- Preconnect for faster font loading -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>

<!-- Inter: Headings and Body -->
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">

<!-- Geist Mono: Data numbers (via CDN since not on Google Fonts) -->
<link href="https://cdn.jsdelivr.net/npm/geist@1.2.2/dist/fonts/geist-mono/style.css" rel="stylesheet">
```

Note: Geist Mono is Vercel's monospace font, not on Google Fonts. Use jsDelivr CDN for the official package. Include weights: 400, 500, 600 for data display variations.

Place these links BEFORE any existing font links (Space Grotesk, DM Sans) to prioritize loading. Keep the existing fonts as fallbacks for any legacy components that might reference them.
  </action>
  <verify>
grep "fonts.googleapis.com.*Inter" index.html returns match
grep "geist-mono" index.html returns match
  </verify>
  <done>
index.html loads Inter from Google Fonts and Geist Mono from jsDelivr CDN
  </done>
</task>

<task type="auto">
  <name>Task 2: Update font tokens in tokens.css</name>
  <files>src/v2/styles/tokens.css</files>
  <action>
Update the FONT TOKENS section in :root to use the new font families:

**Current:**
```css
--font-display: "Space Grotesk", system-ui, sans-serif;
--font-body: "DM Sans", system-ui, -apple-system, ...;
--font-mono: "JetBrains Mono", "SF Mono", ...;
```

**Updated:**
```css
/* ========================================
   FONT TOKENS
   "Rowing Instrument" Typography System
   ======================================== */

/* Display/Headings: Inter Bold for clean, modern titles */
--font-display: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;

/* Body: Inter Regular for readable content */
--font-body: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;

/* Data/Mono: Geist Mono for stroke coach precision numbers */
--font-mono: "Geist Mono", "JetBrains Mono", "SF Mono", ui-monospace, Menlo, Monaco, Consolas, monospace;

/* Legacy fallbacks (keep for backwards compat) */
--font-display-legacy: "Space Grotesk", system-ui, sans-serif;
--font-body-legacy: "DM Sans", system-ui, sans-serif;
```

Add a new token for metrics specifically:
```css
/* Large metrics: Inter Black for hero numbers */
--font-metric: "Inter", system-ui, sans-serif;
```
  </action>
  <verify>
grep 'font-display.*Inter' src/v2/styles/tokens.css returns match
grep 'font-mono.*Geist Mono' src/v2/styles/tokens.css returns match
grep 'font-metric' src/v2/styles/tokens.css returns match
  </verify>
  <done>
tokens.css defines Inter for display/body, Geist Mono for data, and new metric font token
  </done>
</task>

<task type="auto">
  <name>Task 3: Add metric typography classes</name>
  <files>src/v2/styles/typography.css</files>
  <action>
Add new typography classes for the "stroke coach" data display aesthetic:

```css
/* ========================================
   METRIC TYPOGRAPHY - "Stroke Coach" Display
   Large numbers that feel like precision instruments
   ======================================== */

/* Metric Large - 72px hero numbers (boat speed, 2k time) */
.v2 .text-metric-xl {
  font-family: var(--font-metric);
  font-size: 4.5rem; /* 72px */
  line-height: 1;
  font-weight: 900;
  letter-spacing: -0.03em;
  font-variant-numeric: tabular-nums;
  color: var(--color-text-primary);
}

/* Metric Large - 48px secondary metrics (split, rate) */
.v2 .text-metric-lg {
  font-family: var(--font-metric);
  font-size: 3rem; /* 48px */
  line-height: 1.1;
  font-weight: 700;
  letter-spacing: -0.02em;
  font-variant-numeric: tabular-nums;
  color: var(--color-text-primary);
}

/* Metric Medium - 32px dashboard widgets */
.v2 .text-metric-md {
  font-family: var(--font-metric);
  font-size: 2rem; /* 32px */
  line-height: 1.2;
  font-weight: 700;
  letter-spacing: -0.02em;
  font-variant-numeric: tabular-nums;
  color: var(--color-text-primary);
}

/* Metric Small - 24px inline stats */
.v2 .text-metric-sm {
  font-family: var(--font-metric);
  font-size: 1.5rem; /* 24px */
  line-height: 1.25;
  font-weight: 600;
  letter-spacing: -0.01em;
  font-variant-numeric: tabular-nums;
  color: var(--color-text-primary);
}

/* Data Table Numbers - Geist Mono for aligned columns */
.v2 .text-data-lg {
  font-family: var(--font-mono);
  font-size: 1.125rem; /* 18px */
  font-weight: 600;
  letter-spacing: -0.02em;
  font-variant-numeric: tabular-nums;
}

/* Tabular numbers utility - ensure alignment in tables */
.v2 .tabular-nums {
  font-variant-numeric: tabular-nums;
}
```

Update existing .text-data class to use Geist Mono explicitly:
```css
/* Data emphasis - erg times, rankings */
.v2 .text-data {
  font-family: var(--font-mono);
  font-size: 1rem;
  font-weight: 600;
  letter-spacing: -0.02em;
  font-variant-numeric: tabular-nums;
}
```

Also update the display and heading classes to use Inter:

```css
/* Display - Hero text (Inter Black) */
.v2 .text-display {
  font-family: var(--font-display);
  font-size: 2.25rem; /* 36px */
  line-height: 2.5rem; /* 40px */
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--color-text-primary);
}
```
  </action>
  <verify>
grep "text-metric-xl" src/v2/styles/typography.css returns match
grep "font-metric" src/v2/styles/typography.css returns match
grep "tabular-nums" src/v2/styles/typography.css returns multiple matches
  </verify>
  <done>
typography.css includes metric classes (xl/lg/md/sm), data table classes, and tabular-nums utility
  </done>
</task>

</tasks>

<verification>
1. index.html has Google Fonts link for Inter
2. index.html has Geist Mono CDN link
3. tokens.css has --font-display using Inter
4. tokens.css has --font-mono using Geist Mono
5. typography.css has .text-metric-xl through .text-metric-sm classes
6. All metric classes use font-variant-numeric: tabular-nums
</verification>

<success_criteria>
- Inter loads for headings and body text
- Geist Mono loads for data/numbers
- Large metric classes available (72px, 48px, 32px, 24px)
- All numeric classes use tabular-nums for proper alignment
- Typography feels like "precision instrument" for data
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-overhaul/17-02-SUMMARY.md`
</output>
