---
phase: 09-seat-racing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/v2/components/seat-racing/ConfidenceBadge.tsx
  - src/v2/components/seat-racing/RankingsTable.tsx
  - src/v2/components/seat-racing/RankingsChart.tsx
  - src/v2/components/seat-racing/SessionList.tsx
  - src/v2/components/seat-racing/index.ts
autonomous: true

must_haves:
  truths:
    - "Coach can view sorted athlete rankings table"
    - "Confidence badges display tier (PROVISIONAL/LOW/MEDIUM/HIGH)"
    - "Rankings table supports sorting by rating, races, confidence"
    - "Session list shows past seat race sessions"
  artifacts:
    - path: "src/v2/components/seat-racing/ConfidenceBadge.tsx"
      provides: "Visual confidence indicator component"
      min_lines: 30
    - path: "src/v2/components/seat-racing/RankingsTable.tsx"
      provides: "Sortable rankings table with TanStack Table"
      min_lines: 100
    - path: "src/v2/components/seat-racing/SessionList.tsx"
      provides: "List of seat race sessions with dates and details"
      min_lines: 80
    - path: "src/v2/components/seat-racing/index.ts"
      provides: "Barrel exports for all seat racing components"
  key_links:
    - from: "src/v2/components/seat-racing/RankingsTable.tsx"
      to: "useAthleteRatings hook"
      via: "TanStack Query import"
      pattern: "useAthleteRatings"
    - from: "src/v2/components/seat-racing/SessionList.tsx"
      to: "useSeatRaceSessions hook"
      via: "TanStack Query import"
      pattern: "useSeatRaceSessions"
---

<objective>
Create UI components for viewing seat race rankings and session history.

Purpose: Provide read-only display components that show ELO rankings and past sessions. These components address SEAT-07 (confidence intervals display) and SEAT-08 (athlete rankings view).

Output: Reusable components for rankings display and session listing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seat-racing/09-RESEARCH.md

# V1 patterns to reference
@src/components/SeatRacing/RankingsDisplay.jsx

# V2 patterns to follow
@src/v2/components/erg/ErgTestsTable.tsx
@src/v2/components/athletes/AthletesTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConfidenceBadge component</name>
  <files>src/v2/components/seat-racing/ConfidenceBadge.tsx</files>
  <action>
Create a visual badge component that displays confidence level for ELO ratings.

Props:
- `confidence: number` - Confidence score from 0 to 1
- `size?: 'sm' | 'md'` - Badge size (default: 'sm')
- `showLabel?: boolean` - Whether to show text label (default: true)

Confidence tiers (from research):
- 0: UNRATED (gray)
- 0-0.25: PROVISIONAL (red-500)
- 0.26-0.50: LOW (orange-500)
- 0.51-0.75: MEDIUM (yellow-500)
- 0.76-1.0: HIGH (green-500)

Use `getConfidenceLevel()` utility from types/seatRacing.ts.

Styling:
- Rounded pill shape (`rounded-full`)
- Semi-transparent backgrounds matching V2 design tokens
- Small font (`text-xs font-medium`)
- Icon optional: show colored dot indicator when showLabel=false

Example usage:
```tsx
<ConfidenceBadge confidence={0.8} /> // Shows "HIGH" with green background
<ConfidenceBadge confidence={0.3} showLabel={false} /> // Shows orange dot only
```

Follow V2 design tokens (bg-surface, text-primary, etc).
  </action>
  <verify>
File exists and exports ConfidenceBadge component: `ls /home/swd/RowLab/src/v2/components/seat-racing/ConfidenceBadge.tsx`
  </verify>
  <done>
ConfidenceBadge component renders appropriate color and label for each confidence tier.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RankingsTable component with TanStack Table</name>
  <files>src/v2/components/seat-racing/RankingsTable.tsx</files>
  <action>
Create a sortable rankings table using TanStack Table (consistent with Phase 6-7 patterns).

Props:
- `ratings: RatingWithAthlete[]` - Array of ratings with athlete info
- `isLoading?: boolean` - Loading state
- `onRecalculate?: () => void` - Callback for recalculate button

Columns:
1. **Rank** - Computed from array index (1-indexed), width 60px
2. **Athlete** - Last name, First name format, width 200px
3. **Side** - Port/Starboard badge (same colors as lineup builder), width 100px
4. **ELO Rating** - Rounded integer, monospace font, sortable, width 100px
5. **Pieces** - racesCount, sortable, width 80px
6. **Confidence** - ConfidenceBadge component, sortable, width 120px

Features:
- Default sort by rating descending
- Click column headers to sort (asc/desc indicators)
- Hover row highlight
- Empty state when no ratings

Header actions:
- "Recalculate Rankings" button in top-right (calls onRecalculate)
- Filter dropdown for side (All/Port/Starboard)

Use TanStack Table hooks:
- `useReactTable` with `getCoreRowModel`, `getSortedRowModel`
- Column definitions with `accessorKey`, `header`, `cell` renderers
- Sorting state with `onSortingChange`

Styling:
- Follow ErgTestsTable.tsx patterns
- V2 design tokens (bg-surface-elevated, border-border, text-primary)
- Responsive: stack on mobile (<768px)

Rating color scale (from V1 RankingsDisplay):
- >= 1200: text-blue-500 (blade-blue)
- >= 1000: text-primary (normal)
- >= 800: text-orange-500
- < 800: text-muted

Rank highlighting:
- Top 3: bold orange/gold color
- Others: normal weight
  </action>
  <verify>
File exists: `ls /home/swd/RowLab/src/v2/components/seat-racing/RankingsTable.tsx`
  </verify>
  <done>
RankingsTable renders sortable table with all columns, confidence badges, and recalculate button.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SessionList component</name>
  <files>src/v2/components/seat-racing/SessionList.tsx</files>
  <action>
Create a list component showing past seat race sessions.

Props:
- `sessions: SeatRaceSession[]` - Array of sessions
- `isLoading?: boolean` - Loading state
- `onSelectSession?: (sessionId: string) => void` - Navigation callback
- `onDeleteSession?: (sessionId: string) => void` - Delete callback

List item display:
- Date (formatted as "Jan 24, 2026" or relative "2 days ago")
- Boat class badge (8+, 4+, etc)
- Conditions badge if set (calm/variable/rough)
- Location if set
- Piece count ("3 pieces")
- Description truncated to 1 line

Actions per row:
- Click row to select/view details
- Delete button (trash icon) on hover with confirmation

Empty state:
- Illustration or icon
- "No seat races yet"
- "Create your first seat race session to start ranking athletes"

Loading state:
- Skeleton cards (3 rows)

Sorting:
- Default by date descending (most recent first)

Styling:
- Card-based list items
- Hover state with slight background change
- V2 design tokens
- Responsive: full width cards on mobile

Use Framer Motion for list animations (consistent with other V2 lists).
  </action>
  <verify>
File exists: `ls /home/swd/RowLab/src/v2/components/seat-racing/SessionList.tsx`
  </verify>
  <done>
SessionList renders past sessions with date, boat class, conditions, and click-to-select behavior.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create barrel export and RankingsChart placeholder</name>
  <files>
    src/v2/components/seat-racing/RankingsChart.tsx
    src/v2/components/seat-racing/index.ts
  </files>
  <action>
Create RankingsChart component (bar chart showing ELO distribution) and barrel exports.

**RankingsChart.tsx:**
Simple bar chart using recharts (already installed) showing athlete ratings.

Props:
- `ratings: RatingWithAthlete[]` - Array of ratings
- `maxAthletes?: number` - Limit display (default: 10)

Chart:
- Horizontal bar chart
- X-axis: Rating value (domain 800-1200 or auto)
- Y-axis: Athlete last names
- Bar color: Blue with opacity based on confidence (0.3 + confidence * 0.7)
- Tooltip: Shows full name, exact rating, confidence level

Use ResponsiveContainer for auto-sizing.

```tsx
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Cell } from 'recharts';
```

**index.ts:**
Barrel export all components:
```typescript
export { ConfidenceBadge } from './ConfidenceBadge';
export { RankingsTable } from './RankingsTable';
export { RankingsChart } from './RankingsChart';
export { SessionList } from './SessionList';
```
  </action>
  <verify>
Directory and exports exist: `ls /home/swd/RowLab/src/v2/components/seat-racing/`
  </verify>
  <done>
All seat racing components exported from index.ts, RankingsChart renders bar visualization.
  </done>
</task>

</tasks>

<verification>
1. All component files created in src/v2/components/seat-racing/
2. ConfidenceBadge shows correct colors for each tier
3. RankingsTable is sortable by rating, pieces, confidence
4. SessionList shows sessions with proper formatting
5. All components use V2 design tokens
6. Barrel exports work correctly
</verification>

<success_criteria>
- ConfidenceBadge component displays 5 confidence tiers with appropriate colors
- RankingsTable shows sortable athlete rankings with TanStack Table
- SessionList displays past sessions with dates, boat class, conditions
- RankingsChart renders horizontal bar chart of ratings
- All components follow V2 design patterns and tokens
</success_criteria>

<output>
After completion, create `.planning/phases/09-seat-racing/09-02-SUMMARY.md`
</output>
