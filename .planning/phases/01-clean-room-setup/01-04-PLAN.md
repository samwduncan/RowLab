---
phase: 01-clean-room-setup
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "/beta route renders V2 layout with design tokens applied"
    - "Theme switching works (dark/light/field modes)"
    - "V1 app at /app still functions correctly"
    - "Database migration applied successfully"
    - "CSS isolation prevents V1/V2 style conflicts"
  artifacts: []
  key_links: []
---

<objective>
Verify the Phase 1 foundation works end-to-end: V2 routes render, CSS isolation works, theme switching functions, and database schema is updated.

Purpose: Human verification ensures the foundation is solid before building on it in Phase 2. This checkpoint catches integration issues early.

Output:
- Confirmed working V2 entry point at `/beta`
- Confirmed V1 still works at `/app`
- Confirmed database migration applied
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-clean-room-setup/01-01-SUMMARY.md
@.planning/phases/01-clean-room-setup/01-02-SUMMARY.md
@.planning/phases/01-clean-room-setup/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated Verification Checks</name>
  <files></files>
  <action>
Run automated verification before human testing:

**1. Prisma schema validation:**
```bash
npx prisma validate
```
Expected: "The schema at prisma/schema.prisma is valid."

**2. Database migration status:**
```bash
npx prisma migrate status
```
Expected: All migrations applied, no pending migrations.

**3. Build test:**
```bash
npm run build
```
Expected: Build completes without errors.

**4. Check V2 chunks exist:**
```bash
ls -la dist/assets/ | grep -i v2 || echo "V2 chunks may be auto-named"
```
Expected: Separate chunk files for V2 code.

**5. Start dev server for manual testing:**
```bash
npm run dev
```
Keep server running for human verification.

Report results and any errors encountered.
  </action>
  <verify>
All automated checks pass. Dev server is running.
  </verify>
  <done>Automated verification complete. Dev server running for human testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 1 foundation:
- V2 directory structure with design tokens
- Tailwind CSS isolation via `.v2` selector strategy
- V2Layout with theme switching
- /beta route with BetaHome page
- Database schema with new V2 models
  </what-built>
  <how-to-verify>
**Test the V2 entry point:**

1. Open browser to `http://100.86.4.57:3001/beta` (or localhost:3001/beta)
2. Verify you see:
   - "RowLab V2" header with "BETA" badge
   - "RowLab V2 Beta" heading in content area
   - Theme dropdown in header (Dark/Light/Field)

**Test theme switching:**

3. Switch to "Light" theme using dropdown
   - Background should change to white
   - Text should change to dark
4. Switch to "Field" theme
   - Background should be pure black
   - Text should be high contrast white
5. Switch back to "Dark" theme
   - Background should return to void-deep (#08080A)
6. Refresh page - theme should persist

**Test CSS isolation:**

7. Navigate to `http://100.86.4.57:3001/app` (V1)
8. Verify V1 dashboard loads normally with existing styles
9. Navigate back to `/beta`
10. Verify V2 styles are intact (no V1 style bleed)

**Test V1 functionality:**

11. In V1 (`/app`), navigate to a few pages:
    - Athletes page
    - Lineup Builder
    - Settings
12. Verify all V1 pages work correctly
13. Return to `/beta` and confirm V2 still works

**Report results:**

- [ ] V2 renders at /beta with correct styling
- [ ] Theme switching works (all 3 themes)
- [ ] Theme persists across refresh
- [ ] V1 at /app is unaffected
- [ ] No console errors in browser DevTools
  </how-to-verify>
  <resume-signal>
Type "approved" if all checks pass, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
Human verification checkpoint - results reported by user.
</verification>

<success_criteria>
- [ ] Automated Prisma validation passes
- [ ] Database migration applied successfully
- [ ] Build completes without errors
- [ ] Human verified: /beta route renders V2Layout
- [ ] Human verified: Theme switching works (dark/light/field)
- [ ] Human verified: Theme persists across refresh
- [ ] Human verified: V1 at /app is unaffected
- [ ] Human verified: No browser console errors
- [ ] Human verified: CSS isolation working (no style conflicts)
</success_criteria>

<output>
After completion, create `.planning/phases/01-clean-room-setup/01-04-SUMMARY.md`

This completes Phase 1: Clean Room Setup.
</output>
